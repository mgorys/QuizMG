This is a web application, which uses Model-View-Controller (MVC) design patter.

Model part is supported by repository pattern, which contains businness logic with operations that should be performed by it.

Application is connected to a local database.